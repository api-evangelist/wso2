{
  "opencollection": "1.0.0",
  "info": {
    "name": "WSO2 API Manager - Admin Advanced Policy (Collection) Advanced Policy (Collection) Server Startup Healthcheck API",
    "version": "v4"
  },
  "items": [
    {
      "info": {
        "name": "Server Startup Healthcheck",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Health check to check if all the API are deployed during the server startup\n",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://apis.wso2.com/api/am/admin/v4/server-startup-healthcheck",
            "auth": {
              "type": "oauth2",
              "flow": "resource_owner_password_credentials",
              "accessTokenUrl": "https://localhost:9443/oauth2/token",
              "credentials": {
                "clientId": "{{clientId}}",
                "clientSecret": "{{clientSecret}}"
              }
            }
          },
          "docs": "Health check to check if all the API are deployed during the server startup\n"
        }
      ]
    }
  ],
  "bundled": true
}