naftiko: 1.0.0-alpha2
info:
  label: WSO2 API Manager - Admin — Subscription Policy (Collection)
  description: 'WSO2 API Manager - Admin — Subscription Policy (Collection). 2 operations. Lead operation: Get all Subscription
    Throttling Policies. Self-contained Naftiko capability covering one Wso2 business surface.'
  tags:
  - Wso2
  - Subscription Policy (Collection)
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WSO2_API_KEY: WSO2_API_KEY
capability:
  consumes:
  - type: http
    namespace: admin-subscription-policy-collection
    baseUri: https://apis.wso2.com/api/am/admin/v4
    description: WSO2 API Manager - Admin — Subscription Policy (Collection) business capability. Self-contained, no shared
      references.
    resources:
    - name: throttling-policies-subscription
      path: /throttling/policies/subscription
      operations:
      - name: get
        method: GET
        description: Get all Subscription Throttling Policies
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Add a Subscription Throttling Policy
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.WSO2_API_KEY}}'
  exposes:
  - type: rest
    namespace: admin-subscription-policy-collection-rest
    port: 8080
    description: REST adapter for WSO2 API Manager - Admin — Subscription Policy (Collection). One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/throttling/policies/subscription
      name: throttling-policies-subscription
      description: REST surface for throttling-policies-subscription.
      operations:
      - method: GET
        name: get
        description: Get all Subscription Throttling Policies
        call: admin-subscription-policy-collection.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Add a Subscription Throttling Policy
        call: admin-subscription-policy-collection.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: admin-subscription-policy-collection-mcp
    port: 9090
    transport: http
    description: MCP adapter for WSO2 API Manager - Admin — Subscription Policy (Collection). One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: get-all-subscription-throttling-policies
      description: Get all Subscription Throttling Policies
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: admin-subscription-policy-collection.get
      outputParameters:
      - type: object
        mapping: $.
    - name: add-subscription-throttling-policy
      description: Add a Subscription Throttling Policy
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: admin-subscription-policy-collection.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
