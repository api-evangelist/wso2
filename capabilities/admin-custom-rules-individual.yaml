naftiko: 1.0.0-alpha2
info:
  label: WSO2 API Manager - Admin — Custom Rules (Individual)
  description: 'WSO2 API Manager - Admin — Custom Rules (Individual). 3 operations. Lead operation: Get a Custom Rule. Self-contained
    Naftiko capability covering one Wso2 business surface.'
  tags:
  - Wso2
  - Custom Rules (Individual)
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WSO2_API_KEY: WSO2_API_KEY
capability:
  consumes:
  - type: http
    namespace: admin-custom-rules-individual
    baseUri: https://apis.wso2.com/api/am/admin/v4
    description: WSO2 API Manager - Admin — Custom Rules (Individual) business capability. Self-contained, no shared references.
    resources:
    - name: throttling-policies-custom-ruleId
      path: /throttling/policies/custom/{ruleId}
      operations:
      - name: get
        method: GET
        description: Get a Custom Rule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: put
        method: PUT
        description: Update a Custom Rule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: Delete a Custom Rule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.WSO2_API_KEY}}'
  exposes:
  - type: rest
    namespace: admin-custom-rules-individual-rest
    port: 8080
    description: REST adapter for WSO2 API Manager - Admin — Custom Rules (Individual). One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/throttling/policies/custom/{ruleid}
      name: throttling-policies-custom-ruleid
      description: REST surface for throttling-policies-custom-ruleId.
      operations:
      - method: GET
        name: get
        description: Get a Custom Rule
        call: admin-custom-rules-individual.get
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: put
        description: Update a Custom Rule
        call: admin-custom-rules-individual.put
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Delete a Custom Rule
        call: admin-custom-rules-individual.delete
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: admin-custom-rules-individual-mcp
    port: 9090
    transport: http
    description: MCP adapter for WSO2 API Manager - Admin — Custom Rules (Individual). One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: get-custom-rule
      description: Get a Custom Rule
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: admin-custom-rules-individual.get
      outputParameters:
      - type: object
        mapping: $.
    - name: update-custom-rule
      description: Update a Custom Rule
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: admin-custom-rules-individual.put
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-custom-rule
      description: Delete a Custom Rule
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: admin-custom-rules-individual.delete
      outputParameters:
      - type: object
        mapping: $.
