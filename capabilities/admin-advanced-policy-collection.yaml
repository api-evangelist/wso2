naftiko: 1.0.0-alpha2
info:
  label: WSO2 API Manager - Admin — Advanced Policy (Collection)
  description: 'WSO2 API Manager - Admin — Advanced Policy (Collection). 2 operations. Lead operation: Get all Advanced Throttling
    Policies. Self-contained Naftiko capability covering one Wso2 business surface.'
  tags:
  - Wso2
  - Advanced Policy (Collection)
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WSO2_API_KEY: WSO2_API_KEY
capability:
  consumes:
  - type: http
    namespace: admin-advanced-policy-collection
    baseUri: https://apis.wso2.com/api/am/admin/v4
    description: WSO2 API Manager - Admin — Advanced Policy (Collection) business capability. Self-contained, no shared references.
    resources:
    - name: throttling-policies-advanced
      path: /throttling/policies/advanced
      operations:
      - name: get
        method: GET
        description: Get all Advanced Throttling Policies
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Add an Advanced Throttling Policy
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.WSO2_API_KEY}}'
  exposes:
  - type: rest
    namespace: admin-advanced-policy-collection-rest
    port: 8080
    description: REST adapter for WSO2 API Manager - Admin — Advanced Policy (Collection). One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/throttling/policies/advanced
      name: throttling-policies-advanced
      description: REST surface for throttling-policies-advanced.
      operations:
      - method: GET
        name: get
        description: Get all Advanced Throttling Policies
        call: admin-advanced-policy-collection.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Add an Advanced Throttling Policy
        call: admin-advanced-policy-collection.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: admin-advanced-policy-collection-mcp
    port: 9090
    transport: http
    description: MCP adapter for WSO2 API Manager - Admin — Advanced Policy (Collection). One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: get-all-advanced-throttling-policies
      description: Get all Advanced Throttling Policies
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: admin-advanced-policy-collection.get
      outputParameters:
      - type: object
        mapping: $.
    - name: add-advanced-throttling-policy
      description: Add an Advanced Throttling Policy
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: admin-advanced-policy-collection.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
