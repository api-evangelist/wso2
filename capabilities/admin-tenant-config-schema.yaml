naftiko: 1.0.0-alpha2
info:
  label: WSO2 API Manager - Admin — Tenant Config Schema
  description: 'WSO2 API Manager - Admin — Tenant Config Schema. 1 operations. Lead operation: Export a tenant-Config-Schema..
    Self-contained Naftiko capability covering one Wso2 business surface.'
  tags:
  - Wso2
  - Tenant Config Schema
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WSO2_API_KEY: WSO2_API_KEY
capability:
  consumes:
  - type: http
    namespace: admin-tenant-config-schema
    baseUri: https://apis.wso2.com/api/am/admin/v4
    description: WSO2 API Manager - Admin — Tenant Config Schema business capability. Self-contained, no shared references.
    resources:
    - name: tenant-config-schema
      path: /tenant-config-schema
      operations:
      - name: exporttenantconfigschema
        method: GET
        description: Export a tenant-Config-Schema.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.WSO2_API_KEY}}'
  exposes:
  - type: rest
    namespace: admin-tenant-config-schema-rest
    port: 8080
    description: REST adapter for WSO2 API Manager - Admin — Tenant Config Schema. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/tenant-config-schema
      name: tenant-config-schema
      description: REST surface for tenant-config-schema.
      operations:
      - method: GET
        name: exporttenantconfigschema
        description: Export a tenant-Config-Schema.
        call: admin-tenant-config-schema.exporttenantconfigschema
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: admin-tenant-config-schema-mcp
    port: 9090
    transport: http
    description: MCP adapter for WSO2 API Manager - Admin — Tenant Config Schema. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: export-tenant-config-schema
      description: Export a tenant-Config-Schema.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: admin-tenant-config-schema.exporttenantconfigschema
      outputParameters:
      - type: object
        mapping: $.
