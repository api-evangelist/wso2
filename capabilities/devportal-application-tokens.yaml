naftiko: 1.0.0-alpha2
info:
  label: WSO2 API Manager - Developer Portal — Application Tokens
  description: 'WSO2 API Manager - Developer Portal — Application Tokens. 2 operations. Lead operation: Generate Application
    Token. Self-contained Naftiko capability covering one Wso2 business surface.'
  tags:
  - Wso2
  - Application Tokens
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WSO2_API_KEY: WSO2_API_KEY
capability:
  consumes:
  - type: http
    namespace: devportal-application-tokens
    baseUri: https://apis.wso2.com/api/am/devportal/v3
    description: WSO2 API Manager - Developer Portal — Application Tokens business capability. Self-contained, no shared references.
    resources:
    - name: applications-applicationId-keys-keyType-generate-token
      path: /applications/{applicationId}/keys/{keyType}/generate-token
      operations:
      - name: post
        method: POST
        description: Generate Application Token
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: applications-applicationId-oauth-keys-keyMappingId-generate-token
      path: /applications/{applicationId}/oauth-keys/{keyMappingId}/generate-token
      operations:
      - name: post
        method: POST
        description: Generate Application Token
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.WSO2_API_KEY}}'
  exposes:
  - type: rest
    namespace: devportal-application-tokens-rest
    port: 8080
    description: REST adapter for WSO2 API Manager - Developer Portal — Application Tokens. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/applications/{applicationid}/keys/{keytype}/generate-token
      name: applications-applicationid-keys-keytype-generate-token
      description: REST surface for applications-applicationId-keys-keyType-generate-token.
      operations:
      - method: POST
        name: post
        description: Generate Application Token
        call: devportal-application-tokens.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/applications/{applicationid}/oauth-keys/{keymappingid}/generate-token
      name: applications-applicationid-oauth-keys-keymappingid-generate-token
      description: REST surface for applications-applicationId-oauth-keys-keyMappingId-generate-token.
      operations:
      - method: POST
        name: post
        description: Generate Application Token
        call: devportal-application-tokens.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: devportal-application-tokens-mcp
    port: 9090
    transport: http
    description: MCP adapter for WSO2 API Manager - Developer Portal — Application Tokens. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: generate-application-token
      description: Generate Application Token
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: devportal-application-tokens.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: generate-application-token-2
      description: Generate Application Token
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: devportal-application-tokens.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
