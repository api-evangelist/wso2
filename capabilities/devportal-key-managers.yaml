naftiko: 1.0.0-alpha2
info:
  label: WSO2 API Manager - Developer Portal — Key Managers
  description: 'WSO2 API Manager - Developer Portal — Key Managers. 1 operations. Lead operation: Get All Key Managers. Self-contained
    Naftiko capability covering one Wso2 business surface.'
  tags:
  - Wso2
  - Key Managers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WSO2_API_KEY: WSO2_API_KEY
capability:
  consumes:
  - type: http
    namespace: devportal-key-managers
    baseUri: https://apis.wso2.com/api/am/devportal/v3
    description: WSO2 API Manager - Developer Portal — Key Managers business capability. Self-contained, no shared references.
    resources:
    - name: key-managers
      path: /key-managers
      operations:
      - name: get
        method: GET
        description: Get All Key Managers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.WSO2_API_KEY}}'
  exposes:
  - type: rest
    namespace: devportal-key-managers-rest
    port: 8080
    description: REST adapter for WSO2 API Manager - Developer Portal — Key Managers. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/key-managers
      name: key-managers
      description: REST surface for key-managers.
      operations:
      - method: GET
        name: get
        description: Get All Key Managers
        call: devportal-key-managers.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: devportal-key-managers-mcp
    port: 9090
    transport: http
    description: MCP adapter for WSO2 API Manager - Developer Portal — Key Managers. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: get-all-key-managers
      description: Get All Key Managers
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: devportal-key-managers.get
      outputParameters:
      - type: object
        mapping: $.
